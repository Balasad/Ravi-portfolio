{
  "version": 2,
  "cleanUrls": true,
  "outputDirectory": "public",
  "routes": [
    { "src": "/build/(.*)", "dest": "/build/$1" },
    { "src": "/videos/(.*)", "dest": "/videos/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
